# Purr Commands Guide

A Git-like version control system written in Go.

---

## Command Reference

### Main Commands

1. `purr init`
   - Initialize a new Purr repository

2. `purr add .`
   - Stage all files in the current directory

3. `purr add <file1> <file2> ...`
   - Stage specific files by name

4. `purr ls-files`
   - List all files currently staged in the index
   - Subcommands/Flags:
     - `--debug`, `-d` : Show detailed metadata for each file

---

## Command Details

### 1. `purr init`
**Usage:**
```bash
purr init
```
**Description:**
- Creates `.purr` directory structure
- Sets up `objects/`, `refs/heads/`, and `logs/` directories
- Creates an empty index file with valid header
- On Windows: sets the `.purr` folder as hidden
**Example:**
```bash
D:\MyProject> purr init
Initialized empty repository
```

---

### 2. `purr add .`
**Usage:**
```bash
purr add .
```
**Description:**
- Recursively walks through all files in the working directory
- Skips hidden files and directories (starting with `.`)
- Creates blob objects for each file with SHA-1 hashing
- Updates the index with file metadata (SHA-1, timestamps, permissions)
- Only updates files that are new or modified (checks modification time)
- Uses concurrent processing (goroutines) for better performance
**Example:**
```bash
D:\MyProject> purr add .
Files added to index
```
**Technical Details:**
- Stores blobs in `.purr/objects/` with Git-compatible format
- Uses zlib compression for blob storage
- Index tracks: SHA-1, file mode, size, modification time, and more
- Supports cross-platform metadata (Windows, Linux, macOS)

---

### 3. `purr add <file1> <file2> ...`
**Usage:**
```bash
purr add README.md main.go
```
**Description:**
- Stages only the files explicitly listed
- Skips hidden files and directories (starting with `.`)
- Creates blob objects for each file with SHA-1 hashing
- Updates the index with file metadata (SHA-1, timestamps, permissions)
- Only updates files that are new or modified (checks modification time)
**Example:**
```bash
D:\MyProject> purr add README.md main.go
Files added to index
```
**Technical Details:**
- Stores blobs in `.purr/objects/` with Git-compatible format
- Uses zlib compression for blob storage
- Index tracks: SHA-1, file mode, size, modification time, and more
- Supports cross-platform metadata (Windows, Linux, macOS)

---

### 4. `purr ls-files`
**Usage:**
```bash
# Simple mode (default)
purr ls-files

# Detailed mode with full metadata
purr ls-files --debug
purr ls-files -d
```
**Description:**
- Reads the `.purr/index` file
- Displays staged files with their SHA-1 hash and mode
**Simple Mode Output:**
```bash
Found 3 file(s) in index:

a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0 100644 README.md
f6e5d4c3b2a1f6e5d4c3b2a1f6e5d4c3b2a1f6e5 100644 main.go
1234567890abcdef1234567890abcdef12345678 100644 utils.go
```

**Debug Mode Output:**
```bash
Found 3 file(s) in index:

  Path: README.md
  SHA1: a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0
  Mode: 100644
  Size: 1234 bytes
  Mtime: 2025-10-19 14:30:45
  Ctime: 2025-10-19 14:30:45
  Dev: 12345
  Ino: 67890
  UID: 1000
  GID: 1000
  Stage: 0

  Path: main.go
  ...
```
**Flags:**
- `-d, --debug` : Show detailed metadata for each file

---

## Typical Workflow

```bash
# 1. Initialize a new repository
purr init

# 2. Stage all files
purr add .

# 3. Stage specific files
purr add README.md main.go

# 4. View staged files
purr ls-files

# 5. View detailed file information
purr ls-files --debug
```

---

## Technical Architecture

### Directory Structure
```
.purr/
├── index          # Binary index file (staged files metadata)
├── objects/       # Blob storage (SHA-1 named files)
│   ├── ab/
│   │   └── cdef...
│   └── ...
├── refs/
│   └── heads/     # Branch references
└── logs/          # Operation logs
```

### Index Format
- **Header:** 12 bytes
  - Magic: `"DIRC"` (4 bytes)
  - Version: `2` (4 bytes, big-endian)
  - Entry count (4 bytes, big-endian)
- **Entries:** Variable length per file
  - Metadata: 62 bytes (timestamps, permissions, size, SHA-1, etc.)
  - Path length: 2 bytes
  - Path data: variable
  - Padding: aligned to 8-byte boundary

### Blob Format (Git-compatible)
- Header: `blob <size>\0`
- Content: raw file data
- Storage: zlib-compressed
- Naming: First 2 chars of SHA-1 as directory, rest as filename

---

## Platform Support

- ✅ **Windows** (with hidden folder support)
- ✅ **Linux** (full metadata support)
- ✅ **macOS** (Darwin - full metadata support)

---

## Coming Soon

- `.purrignore` - Ignore patterns support
- `purr commit` - Create commits
- `purr status` - Show working directory status
- `purr log` - View commit history
- `purr diff` - Show changes between commits

---

## Notes

- All commands must be run from within a Purr repository (initialized with `purr init`)
- Hidden files and directories (starting with `.`) are automatically excluded
- The system uses Git-compatible blob storage for potential interoperability

---

*Last updated: October 19, 2025*